# Copy this file to .env for local development
# NEVER commit .env to GitHub

DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres
JWT_SECRET=isi_dengan_random_string_minimal_64_karakter
JWT_EXPIRES_IN=7d
NODE_ENV=production

# Email (optional — uses Ethereal test account if not set)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Encryption key (uses JWT_SECRET if not set)
ENCRYPTION_KEY=
